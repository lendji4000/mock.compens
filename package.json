{
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "server": "json-server db.json --watch --port 5000",
    "server2": "json-server db.json --routes routes.json --watch --port 5000"
  }
}
